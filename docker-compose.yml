services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      # Override to speed up or slow down the simulated per-URL delay.
      MAX_CHECK_DELAY_MS: "10000"

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
